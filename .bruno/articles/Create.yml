info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{protocol}}://{{host}}:{{port}}/api/{{version}}/articles"
  body:
    type: json
    data: |-
      {
        "title": "test",
        "link": "https://this.is.a.test",
        "liked": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
