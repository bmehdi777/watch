info:
  name: GET many
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{host}}:{{port}}/api/{{version}}/articles?sort=asc"
  params:
    - name: sort
      value: asc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
