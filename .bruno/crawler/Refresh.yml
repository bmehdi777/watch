info:
  name: Refresh
  type: http
  seq: 1

http:
  method: POST
  url: "{{protocol}}://{{host}}:{{port}}/{{version}}/crawler"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
