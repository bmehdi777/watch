info:
  name: Create
  type: http
  seq: 5

http:
  method: POST
  url: 1e72e3e7-578c-4b26-bead-f015f95be2b0{{protocol}}://{{host}}:{{port}}/api/{{version}}/sources
  body:
    type: json
    data: |-
      {
        "title": "Slack Blog",
        "blog_url": "https://slack.engineering/",
        "rss_url": "https://slack.engineering/feed/",
        "enabled": true
      }
  auth: inherit

runtime:
  variables:
    - name: sourceID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
