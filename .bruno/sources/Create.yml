info:
  name: Create
  type: http
  seq: 5

http:
  method: POST
  url: "{{protocol}}://{{host}}:{{port}}/{{version}}/sources"
  body:
    type: json
    data: |-
      {
        "title": "Cloudflare Blog",
        "blog_url": "https://blog.cloudflare.com/",
        "rss_url": "https://blog.cloudflare.com/rss",
        "enabled": true
      }
  auth: inherit

runtime:
  variables:
    - name: sourceID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
