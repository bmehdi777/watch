info:
  name: Remove
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{protocol}}://{{host}}:{{port}}/{{version}}/sources/{{sourceID}}"
  auth: inherit

runtime:
  variables:
    - name: sourceID
      value: 0c5eca85-4354-4e99-b3db-091c536b80b4

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
