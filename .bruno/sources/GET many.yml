info:
  name: GET many
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{host}}:{{port}}/{{version}}/sources"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
