info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{protocol}}://{{host}}:{{port}}/api/{{version}}/models"
  body:
    type: json
    data: |-
      {
        "name": "qcwind/qwen3-8b-instruct-Q4-K-M",
        "display_name": "Default summarize"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
