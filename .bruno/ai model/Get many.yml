info:
  name: Get many
  type: http
  seq: 1

http:
  method: GET
  url: "{{protocol}}://{{host}}:{{port}}/api/{{version}}/models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
